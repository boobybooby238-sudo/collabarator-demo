The first useEffect will run at first mounting and and  if there is a state in the component is the state changes the useEffect will run 
UseEffect accepts call back function
If the pgae refreshes the component get mounted.
The useEffect accepts second argument as dependency in form of array means is the dependency value get changed the useEffect runs if the dependency is an empty array then the useEffect will run onlu 1 time at the time of mounting means refresh only 
When states changes component get rerendered then the useEffect runs